$('.js-form').html('<%= escape_javascript render('form') %>');
ReactDOM.render(
  React.createElement(Photos, {photos: <%= raw @assets.to_json %>, url: "<%= assets_path(event_id: @event.id) %>"}),
  document.getElementById('js-assets')
);
