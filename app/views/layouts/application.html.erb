<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>iCal</title>
    <%= stylesheet_link_tag 'reset', 'home' %>
    <%= csrf_meta_tag %>
    <style>
    <% @actividades.each do |i| %>
      .activity<%= i.id %> {background: #<%= i.color %>;}
    <% end %>
    </style>
  </head>
  <body>
    <%= yield %>

    <%= javascript_include_tag 'jquery.min', 'rails', 'home' %>
  </body>
</html>
