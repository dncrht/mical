- content_for :header_day do
  .col-sm-3

- content_for :header_year do
  .col-sm-6

%p Your password has been reset. Choose a new password below.

.row.justify-content-center
  = form_with scope: :user, class: 'col-4', url: user_password_path(@user, token: @user.confirmation_token), method: :patch do |f|

    .form-group
      = f.label :password, 'Choose password'
      = f.password_field :password, class: 'form-control'

    = f.submit 'Save this password', class: 'btn btn-primary'
